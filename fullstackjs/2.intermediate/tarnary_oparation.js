let password = 2;
function passwordCheaker(ps) {
  return password === 8 ? "Strong password" : "Password should be 8 charecter";
}

const res = passwordCheaker(2);
console.log(res);

const age = 10;
const isAdult = age >= 18 ? "adult" : "Not an adult";

console.log(isAdult);

//challanges
money = true;

const result = money ? "Buy Product" : "They should bring money!";
console.log(result);
